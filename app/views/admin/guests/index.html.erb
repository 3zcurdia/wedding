<p style="color: green"><%= notice %></p>

<h1>Guests</h1>

<div id="guests">
  <% @guests.each do |guest| %>
    <%= render guest %>
    <p>
      <%= link_to "Show this guest", [:admin, guest] %>
    </p>
  <% end %>
</div>

<%= link_to "New guest", new_admin_guest_path %>
