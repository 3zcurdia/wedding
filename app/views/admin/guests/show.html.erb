<%= render @guest %>

<div class="flex flex-row gap-2">
  <%= link_to "Edit", edit_admin_guest_path(@guest), class: "text-sky-600 hover:cursor-pointer" %>
  <%= button_to "Destroy", [:admin, @guest], method: :delete, class: "text-red-700 hover:cursor-pointer" %>
</div>
