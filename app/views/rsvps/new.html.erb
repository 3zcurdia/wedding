<div class="flex flex-col min-h-dvh mx-auto p-4">
  <div class="mt-6 py-4">
    <p class="text-5xl font-serif font-extrabold text-green-900 capitalize text-center">Confirma tú asistencia</p>
  </div>
  <div class="mt-6">
    <p class="text-3xl font-serif mb-2 font-extrabold">¿Cuándo?</p>
    <p class="font-sans">Sabado 27 de Julio 2024</p>
  </div>
  <div class="mt-6">
    <p class="text-3xl font-serif mb-2 font-extrabold">Boletos</p>
    <p class="font-sans">Para confirmar tu asistencia marca el numero de boletos que vas a utilizar.</p>
  </div>
  <%= form_with(model: @guest, url: rsvp_path, class: "w-full") do |form| %>
    <%= form.text_field :phone, placeholder: "(55)-55-55-5555", class: "mt-2 border-1 border-gray-300 rounded-lg py-2 px-5 w-full", value: number_to_phone(@guest.phone) %>
    <%= form.number_field :confirmed_plus_ones, min: 0, max: @guest.plus_ones_count, class: "mt-2 border-1 border-gray-300 rounded-lg py-2 px-5 w-full" %>
    <%= form.submit "Confirmar asistencia", class: "primary-button font-bold rounded-tl-xlg rounded-br-lg my-5" %>
  <% end %>
</div>
